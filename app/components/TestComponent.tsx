import React, { ReactElement } from "react";
import styles from "./testcomponent.module.scss";

const Footer = (): ReactElement => (
  <div className={styles.main}>TestComponent.tsx</div>
);

export default Footer;
